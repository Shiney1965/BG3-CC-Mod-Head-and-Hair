<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:System="clr-namespace:System;assembly=mscorlib"
                    xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
                    xmlns:ls="clr-namespace:ls;assembly=Code"
                    mc:Ignorable="d">

    <!-- CCNS LABEL BUILD (controller variant). Identical to Lib_Keyboard.xaml: red proof border
         removed, tile wrapped in a StackPanel with a name label (CCNS_NameLabel) beneath. -->
    <DataTemplate x:Key="CustomBrushIconTemplate" DataType="{x:Type ls:VMSelectable}">
        <StackPanel x:Name="ccnsRoot" Orientation="Vertical" HorizontalAlignment="Center">
            <Grid x:Name="base" HorizontalAlignment="Center">
                <Grid.ToolTip>
                    <ls:LSTooltip Content="{Binding DataContext.Name, ElementName=base}"/>
                </Grid.ToolTip>
                <Rectangle x:Name="icon" Fill="{Binding Icon}" Width="196" Height="196" RadiusX="20" RadiusY="20"/>
                <ls:LSNineSliceImage x:Name="frame" Visibility="Hidden" ImageSource="{StaticResource IconGridSelector}" Slices="32,32,32,32" Padding="16" Width="216" Height="216"/>
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="MouseLeftButtonUp">
                        <ls:LSPlaySound Sound="UI_Shared_Panel_Select"/>
                    </b:EventTrigger>
                    <b:EventTrigger EventName="MouseLeftButtonDown">
                        <ls:LSPlaySound Sound="UI_Shared_Secondary_Press"/>
                    </b:EventTrigger>
                    <b:EventTrigger EventName="MouseEnter">
                        <ls:LSPlaySound Sound="UI_Shared_Hover"/>
                    </b:EventTrigger>
                </b:Interaction.Triggers>
            </Grid>
            <TextBlock x:Name="CCNS_NameLabel"
                       Text="{Binding DataContext.Name, ElementName=base}"
                       Foreground="White" FontSize="18" TextAlignment="Center"
                       TextWrapping="Wrap" MaxWidth="196" Margin="0,2,0,10"
                       HorizontalAlignment="Center"/>
        </StackPanel>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}" Value="True">
                <Setter TargetName="frame" Property="Visibility" Value="Visible"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListBoxItem}},Path=IsMouseOver}" Value="True">
                <Setter TargetName="frame" Property="Visibility" Value="Visible"/>
                <Setter TargetName="frame" Property="ImageSource" Value="pack://application:,,,/Core;component/Assets/CC/selector_CC_h.png"/>
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

</ResourceDictionary>
